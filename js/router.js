const Router={routes:{},current:'home',page(name,renderFn){this.routes[name]=renderFn},async go(route){route=route||'home';if(window.location.hash!=='#'+route)window.location.hash=route;await this._render(route)},async _render(route){const container=document.getElementById('mainContent');if(!container)return;document.querySelectorAll('.doc-viewer').forEach(v=>v.remove());document.body.style.overflow='';document.querySelectorAll('.md3-nav-item').forEach(item=>item.classList.toggle('active',item.dataset.route===route));const titles={home:'EMS Минск',orders:'Приказы',guidelines:'Клинические рекомендации',calculators:'Калькуляторы',reference:'Справочная информация'};const titleEl=document.getElementById('pageTitle');if(titleEl)titleEl.textContent=titles[route]||'EMS Минск';container.innerHTML='<div class="md3-page" style="text-align:center;padding:40px;color:var(--md-on-surface-variant)">Загрузка...</div>';const fn=this.routes[route];if(typeof fn==='function'){try{await fn(container)}catch(err){console.error('Router error:',route,err);container.innerHTML=`<div class="md3-page"><p style="color:var(--md-error)">Ошибка: ${err.message}</p></div>`}}else{if(typeof this.routes['home']==='function'){await this.routes['home'](container);route='home';window.location.hash='home'}else{container.innerHTML='<div class="md3-page"><p style="color:var(--md-error)">Страница не найдена</p></div>'}}this.current=route;window.scrollTo({top:0});closeDrawer()},start(){if(Object.keys(this.routes).length===0){console.error('No routes registered!');return}window.addEventListener('hashchange',()=>{const route=window.location.hash.replace('#','')||'home';this._render(route)});document.querySelectorAll('.md3-nav-item').forEach(item=>{item.addEventListener('click',(e)=>{e.preventDefault();const route=item.dataset.route;if(route)this.go(route)})});const initial=window.location.hash.replace('#','')||'home';this._render(initial)}};
